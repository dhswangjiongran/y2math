# Find mode of n numbers

import statistics

A = [19, 18, 46, 18, 18, 19, 18, 19, 18]
print(statistics.mode(A))

# the above only works if there is only 1 mode
# what if there is more than 1 mode?

if (statistics.mode(A)>2)
Print = both.mode(A)
